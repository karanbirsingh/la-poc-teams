{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Azure Bot Service Configuration for Okta OAuth - REFERENCE TEMPLATE",
  
  "bot": {
    "name": "{{PREFIX}}-bot",
    "resourceGroup": "{{YOUR_RESOURCE_GROUP}}",
    "subscriptionId": "{{YOUR_SUBSCRIPTION_ID}}",
    "location": "global",
    "sku": "F0",
    "kind": "azurebot",
    "msaAppType": "SingleTenant",
    "msaAppId": "{{BOT_APP_ID}}",
    "msaAppTenantId": "{{AZURE_TENANT_ID}}",
    "msaAppSecret": "{{BOT_APP_SECRET}}",
    "endpoint": "https://{{YOUR_TUNNEL_OR_APP}}.azurewebsites.net/api/messages",
    "channels": [
      "webchat",
      "directline", 
      "msteams"
    ]
  },
  
  "oauthConnection": {
    "name": "okta",
    "serviceProvider": {
      "id": "8379c6d2-b262-4d4f-b89b-68dc5b5f5482",
      "displayName": "Oauth 2 Generic Provider"
    },
    "clientId": "{{OKTA_CLIENT_ID}}",
    "clientSecret": "{{OKTA_CLIENT_SECRET}}",
    "scopes": "openid profile email",
    "parameters": {
      "authorizationUrl": "https://{{YOUR_ORG}}.okta.com/oauth2/default/v1/authorize",
      "tokenUrl": "https://{{YOUR_ORG}}.okta.com/oauth2/default/v1/token",
      "refreshUrl": "https://{{YOUR_ORG}}.okta.com/oauth2/default/v1/token"
    }
  },
  
  "notes": {
    "genericOAuth2Provider": "Used because Azure Bot Service doesn't have a native Okta provider. Service Provider ID 8379c6d2-b262-4d4f-b89b-68dc5b5f5482 is the Generic OAuth 2 provider.",
    "singleTenant": "Bot is configured as SingleTenant - only users from the specified tenant can use it.",
    "endpoint": "Update this to your devtunnel URL or deployed app URL.",
    "secrets": "All values with {{...}} are placeholders - they will be generated by the deploy scripts."
  }
}
