{
  "_comment": "Fill in ONLY the required fields. Everything else is auto-generated.",

  "okta": {
    "_comment": "Get API token from: Okta Admin > Security > API > Tokens > Create Token. The domain is without -admin suffix",
    "domain": "YOUR_ORG.okta.com",
    "apiToken": "YOUR_OKTA_API_TOKEN"
  },

  "azure": {
    "_comment": "subscriptionId optional (uses current az login). resourceGroup is REQUIRED.",
    "subscriptionId": "",
    "resourceGroup": "YOUR_RESOURCE_GROUP",
    "location": "eastus"
  },

  "resources": {
    "_comment": "Optional. Leave empty for auto-generated names with 'okta' prefix.",
    "prefix": "",
    "botName": ""
  }
}
